# Mini War 고정 시점 규격

이 문서는 앞으로 추가되는 모든 건물, 캐릭터, 차량, 장식물의 공통 시점 규칙이다.
새 요소를 제작하거나 코드에 등록할 때 임의의 각도를 사용하지 않는다.

## 1. 공식 시점

- 월드 좌표와 이동 계산: 직교 탑뷰
- 그래픽 표현: 고정 3/4 탑뷰
- 카메라 회전: 사용하지 않음
- 건물 정면: 항상 화면 아래쪽
- 보이는 면: 지붕 + 정면 + 오른쪽 측면
- 깊이 기준: 바닥 또는 발의 월드 Y 좌표
- 그림자 방향: 오른쪽 아래

공통 값은 `visual-config.js`의 `VISUAL_STYLE`에서만 수정한다.

## 2. 기준점

### 건물

- 게임 좌표는 건물 점유 격자의 중앙이다.
- 렌더링 깊이는 건물 그림의 바닥 중앙을 사용한다.
- 지붕, 깃발, 굴뚝은 점유 격자 밖으로 위쪽에 나와도 된다.
- 출입구와 작업 위치는 `VISUAL_METADATA`에 등록한다.

### 캐릭터

- 게임 좌표는 양발 중앙이다.
- 이미지 크기나 장비가 달라져도 발 위치를 바꾸지 않는다.
- 주민, 벌목꾼, 건설공, 병사 모두 같은 기준을 사용한다.

## 3. 건물 메타데이터 필수 항목

새 건물은 `visual-config.js`에 다음 정보를 먼저 추가한다.

```js
new_building: {
  kind: "building",
  footprint: { columns: 3, rows: 2 },
  anchor: { x: 0.5, y: 1 },
  entrance: { x: 0.5, y: 1, offsetY: 10 },
  workPoints: [
    { x: 0.25, y: 1, offsetY: 14 },
    { x: 0.75, y: 1, offsetY: 14 },
  ],
}
```

- `footprint`: 건설 격자 점유 크기
- `anchor`: 바닥 중앙 기준점
- `entrance`: 주민과 병력이 출입하는 위치
- `workPoints`: 작업자, 자재, 훈련 등의 실제 행동 위치
- `visualBounds`: 실제 그림 크기가 코드의 width/height와 다를 때만 사용
- `sortOffsetY`: 특수한 경우 깊이 정렬 기준을 직접 보정할 때만 사용

## 4. 렌더링 규칙

모든 건물과 캐릭터는 `render-system.js`의 `VisualRenderSystem`을 통해 그린다.
종류별로 건물을 전부 그린 뒤 사람을 전부 그리는 방식을 사용하지 않는다.

렌더 순서는 다음 값으로 자동 결정된다.

1. 레이어
2. 바닥/발의 월드 Y 좌표
3. 같은 위치일 경우 등록 순서

따라서 캐릭터가 건물 뒤로 이동하면 건물에 가려지고, 앞으로 이동하면 건물 앞에 보인다.

## 5. 실제 동작 위치

일꾼이나 병사의 출발 위치를 숫자로 직접 더하지 않는다.
다음 함수를 사용한다.

```js
game.getVisualInteractionPoint(building, "entrance");
game.getVisualInteractionPoint(building, "work", 0);
game.getVisualInteractionPoint(building, "anchor");
```

건물 디자인이나 크기가 바뀌어도 메타데이터만 수정하면 인물 동선이 함께 맞춰진다.

## 6. F3 시점 디버그

게임 실행 중 `F3`을 누르면 다음 정보가 표시된다.

- 파랑: 건물 점유 격자
- 빨강: 바닥/발 기준점
- 노랑: 출입구
- 초록: 작업 위치
- 텍스트: 깊이 정렬에 사용하는 Y 값

새 요소를 추가한 뒤 반드시 F3 화면으로 기준점을 검사한다.

## 7. 신규 요소 추가 순서

1. 격자 점유 크기 결정
2. `visual-config.js`에 메타데이터 등록
3. 공통 시점으로 이미지 또는 Canvas 도형 제작
4. 건물 바닥/캐릭터 발 기준점 설정
5. 출입구와 작업 위치 등록
6. `VisualRenderSystem`에 렌더 항목 등록
7. 실제 이동과 작업은 `getVisualInteractionPoint()`에 연결
8. F3 디버그로 확인
9. 줌인·줌아웃과 여러 방향 이동 점검

이미지를 먼저 만든 뒤 좌표를 억지로 맞추지 않는다.

## 병영 집결지 규칙

- 훈련 완료 병사는 정착 본부가 아니라 훈련 병영의 출입구에서 생성한다.
- 생성된 병사는 병영 앞 집결 깃발의 대형 슬롯으로 직접 이동한다.
- 집결 중인 병력은 `awaitingOrder` 상태이며 전선을 자동으로 향하지 않는다.
- 집결 깃발을 눌렀을 때만 출격 명령 UI와 동부·북부 전선 화살표를 표시한다.
- 출격 병력은 집결지 → 본부 남측 도로 허브 → 전선 진입 도로의 웨이포인트를 따라 이동한다.
- 전략 줌에서는 집결·행군 병력을 개별 점이 아니라 인원수가 표시된 부대 아이콘으로 묶어 표현한다.
